@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(termin_base REQUIRED)

if(@TERMIN_MODULES_BUILD_PYTHON@)
    find_dependency(Python COMPONENTS Development REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/termin_modulesTargets.cmake")
