# termin-pga

Archived projective-geometric algebra and geometry helpers for Termin.

This package preserves the old `termin.ga201`, `termin.geomalgo`,
`termin.algeom`, `termin.closest`, and `termin.solve` modules outside
`termin-app`. The code is kept for compatibility and possible future work; it
is not part of the editor layer.
