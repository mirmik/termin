# Known Bugs

## Minor

- [ ] Галочка "Block in Editor" не сериализуется
- [ ] FrameGraph Debugger: точки "между пассами" отсортированы не в порядке заполнения
- [ ] FrameGraph Debugger: при закрытии остаётся артефакт — небольшое чёрное окно (вероятно SDL window)
- [ ] SceneTree: после добавления entity и по умолчанию все иерархии раскрываются полностью
- [ ] При создании файла ProjectBrowser сваливается в корень
- [ ] После drag-drop prefab в сцену материал гизмо становится чёрным (связано с обработкой текстур/контекста)

## Major

- [ ] Первые кадры после старта редактора игнорируют освещение — указывает на проблему в инициализации рендер-пайплайна
	Наблюдение: Флип текстуры также ломает рендеринг на несколько кадров.

## Deferred (отложено)

(пока пусто)
