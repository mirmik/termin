# termin-components-tween

`termin-components-tween` содержит scene-компонент `TweenManagerComponent`.

Компонент живёт в модуле `termin.tween.component`, но поставляется отдельно от
ядра `termin-tween`, чтобы чистый tween-слой не зависел от `termin.scene`.
