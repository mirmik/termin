"""Editor compatibility package.

The Qt editor was removed; this package keeps legacy entry points delegating to
the native editor. The legacy tcgui frontend remains available explicitly
through ``termin_editor --ui=tcgui`` during the retirement window.
"""
