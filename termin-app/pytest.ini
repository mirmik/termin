[pytest]
markers =
    full: slower integration/smoke tests excluded from the default working set
filterwarnings =
    ignore:Using SDL2 binaries from pysdl2-dll:UserWarning
norecursedirs =
    install
    examples
    build*
    .git
